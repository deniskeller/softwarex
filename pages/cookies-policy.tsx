import { Landing } from '@layouts/index';
import { CookiesPolicy } from '@view/landing';

const CookiesPolicyPage = () => {
  return (
    <Landing theme="light">
      <CookiesPolicy />
    </Landing>
  );
};

export default CookiesPolicyPage;
