import { Landing } from '@layouts/index';
import { PersonalProfile } from '@view/landing';

const PersonalProfilePage = () => {
  return (
    <Landing>
      <PersonalProfile />
    </Landing>
  );
};

export default PersonalProfilePage;
